# Core Data Processing
pandas>=2.0.0
pyarrow>=12.0.0
numpy>=1.24.0
python-dateutil>=2.8.0

# Fast Data Loading
polars>=0.18.0
fastparquet>=2023.4.0

# Configuration & Validation
pydantic>=2.0.0

# Development & Debugging
ipython>=8.14.0
jupyter>=1.0.0

# Code Style & Formatting
autopep8>=2.0.0
isort>=5.12.0

# Data Visualization
plotly>=5.15.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Console
rich>=14.2.0
psutil>=7.1.0

# Testing
pytest>=7.0.0

# Async & Networking (Kraken WebSocket)
websockets>=12.0
aiohttp>=3.9.0

# Task Scheduling
apscheduler>=3.10.0

# Status API — loopback only; a reverse proxy terminates TLS in front of it.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Shared auth — bearer tokens with per-surface grants, one implementation with the
# Testing IDE and the RAG engine. Pinned by tag; in development it is installed
# editable from the sibling checkout.
finiex-auth @ git+https://github.com/dc-deal/finiex-modules-auth.git@v0.3.0
